services:
  fcc-proxy:
    image: free-claude-code:local
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fcc-proxy
    ports:
      - "127.0.0.1:8082:8082"
    env_file:
      - .env
    restart: unless-stopped
